# sockets

Phase 9. Socket.IO namespaces/gateways: `chat.gateway.js` (conversation-scoped messaging), `tracking.gateway.js` (live GPS feed for super-admin Tracking.jsx). Wired up in `server.js` once this phase starts (currently a plain HTTP server, no socket attached yet).
