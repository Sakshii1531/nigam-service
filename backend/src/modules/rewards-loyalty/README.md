# rewards-loyalty

**Coupons are built** (Phase 5): flat rupee-amount discount, admin-editable, resolved server-side by `order.service.js` (never a client-supplied discount).

**Deferred** (deliberately, see `../../../docs/DATA_MODEL.md` Phase 5 addendum): membership tiers, referrals, spin-wheel, loyalty milestones — the models exist (Phase 1) but have no service/routes layer yet. See BACKEND_CONTEXT.md §3.11, §6.3.
