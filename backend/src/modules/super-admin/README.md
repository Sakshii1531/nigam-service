# super-admin

Phase 8. Platform-level entities not owned by any brand: `Brand`, `City`, `ServicePartner`, `ASM`, the auto-assignment scoring engine, `LiveTracking`, `Escalation`, `AuditLog`, `SparePartCatalog`, `PlatformSettings`, finance (`BillingTransaction`, `Revenue`, `Payout`, `GatewayTransaction`), CMS/app-customization (writes to the `catalog` module), loyalty config, platform-wide RBAC. See BACKEND_CONTEXT.md §6.
