# auth

Phase 3. Role-parameterized auth (`customer | technician | brand_admin | super_admin`): login, OTP send/verify, forgot/reset password, JWT issuance, RBAC (`roles`, `permissions`, `role_permissions`, `user_roles`). See BACKEND_CONTEXT.md §2, §6.4.
