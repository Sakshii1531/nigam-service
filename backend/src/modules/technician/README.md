# technician

Phase 6 — the largest, highest-risk module (mirrors `ActiveJob.jsx`, the biggest file in the frontend). `Technician` profile, `Job` (ServiceRequest/Booking joined with tech-facing metadata) and its full state machine incl. the revisit sub-flow, `TechInventoryItem`, `PartOrder`, tech-side `Claim`, `EarningsTally`, `Payout`, Academy content, `Announcement`. See BACKEND_CONTEXT.md §4.
