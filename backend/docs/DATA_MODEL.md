# Data Model (Phase 1)

64 collections, implemented as Mongoose schemas under `src/modules/*/*.model.js`. This
doc records the *decisions* (embed vs. reference, relationships, indexes) — field-level
detail already lives in `../../BACKEND_CONTEXT.md` §3–§6 and isn't repeated here.

## Conventions

- **`applyStandardPlugins(schema, { prefix })`** (`src/modules/shared/plugins.js`) is applied to every top-level collection:
  - adds a `humanId` field (unique, sparse) carrying the frontend's existing ID scheme (`NCC-...`, `SR-...`, etc. — see `ID_PREFIXES` in `src/config/constants.js`). Generation itself (the `Counter`-backed atomic increment) is a Phase 2 service; Phase 1 just reserves the field.
  - shapes `toJSON` so `_id` → `id` and `__v` is dropped, so every API response has a consistent shape without each controller doing it by hand.
- **Embed vs. reference** — embedded wherever a sub-document has no independent lifecycle and is always read/written with its parent (e.g. `Address` inside `User`, `ServiceRequest.timeline`, all of `Job`'s work-in-progress state). Referenced (own collection, `ObjectId` + `ref`) wherever a document is independently queried, paginated, or has its own lifecycle (e.g. `Technician`, `Invoice`, `Product`).
- **Snapshots over live refs for money/legal fields** — `Booking.service`, `Booking.address`, `Order.items[].name/price` are copied at write time rather than referencing the live `ServiceCatalogItem`/`Address`/`Product`, so editing the catalog or an address later can't retroactively change a customer's existing booking/order.
- **Three deliberate "merge instead of duplicate" calls**, each documented inline in the model file: `Role` (single collection for both super-admin's platform-wide roles and brand-admin's brand-scoped roles, disambiguated by `scope`+`brand` — plan draft called for two collections; a discriminator gives the same isolation with far less duplicated CRUD/validation code), `Escalation` (same pattern for brand-level vs. platform-level escalations), `Claim` (single collection for both customer-raised and technician-raised claims, via `raisedByModel`/`refPath` polymorphism).
- **Transactions**: `Payment` + `WalletLedger` + `Booking`/`Order` status writes must happen inside one Mongo session/transaction (Phase 5) — requires the replica-set connection noted in `.env.example`.

## Collections by module

### `auth/`
| Collection | Kind | Notes |
|---|---|---|
| `User` | top-level | One collection for all 4 roles (`role` enum), not 4 separate tables — auth/session logic is identical, only the extra fields differ (customer-only: coins/referral/membership; brand_admin-only: `brand`). Embeds `Address[]`. |
| `Permission` | top-level | Seeded once; keyed by `key` (e.g. `invoices:export`). |
| `Role` | top-level | See "merge instead of duplicate" above. |
| `Otp` | top-level, TTL | Auto-expires (`expiresAt` TTL index). |
| `RefreshToken` | top-level, TTL | Revocation list for JWT refresh tokens. |
| `Address` (`address.schema.js`) | embedded only | Lives inside `User.addresses[]`, `Booking.address`, `Order.address` (snapshot). |

### `catalog/`
`Category` → `ProductType` (ref category) → `ServiceCatalogItem` (ref category). Replaces `frontend/src/data/bookingCatalog.js`; written to by super-admin CMS endpoints (Phase 8), read by customer booking flow (Phase 4).

### `booking/`
`Booking` — snapshots `service` and `address`; refs `user`, `technician` (nullable until auto-assigned), `serviceRequest` (set once the booking spawns one).

### `service-requests/`
`OwnedAppliance` (a customer's registered appliance instance; `warrantyStatus` is a cached field recomputed by `warrantyEngine`, Phase 2) and `ServiceRequest` — the central ticket entity, embeds its own `timeline[]`, referenced by Invoice/Review/GeneratedDocument/ReverseLogisticsReturn/Escalation/Job.

### `warranty-amc-exchange/`
`ExtendedWarrantyOrder`, `AMCPlan` (catalog of plan tiers) → `AMCSubscription` (a customer's active plan) → `AMCVisit` (one per scheduled/completed visit), `Claim` (polymorphic `raisedBy`), and the Exchange sub-domain: `ExchangeQuestionSet` (embeds its `questions[]`, `deductions` as a `Map`), `ExchangeCampaign`, `ExchangeProductConfig` (ties a `Product` to a question set + campaign), `ExchangeRequest` (a customer's trade-in valuation; stores the computed `estimatedValue` rather than recomputing it later, so a config change can't retroactively alter a quoted value).

### `buy-commerce/`
`Product` (new + refurbished, one collection distinguished by `condition`), `Cart` (one doc per user, embedded `items[]`), `Wishlist` (many-to-many `user`×`product` rows — unlike Cart, no reason to embed since it's never edited as a batch), `Order` (embeds item snapshots + address snapshot).

### `payments-wallet/`
`Payment`, `WalletLedger` (append-only; `User.walletCoins` is a cache kept in sync inside the same transaction as each ledger write), `PaymentMethod`.

### `rewards-loyalty/`
`Coupon`, `Membership` (plan tiers), `SpinWheelConfig` (singleton-ish doc, embeds `segments[]` since probabilities are validated as a set, not per-row), `Referral`, `LoyaltyMilestone`.

### `notifications/`
`Notification` (either `recipient` (single user) or `broadcastRole` (platform-wide push) — not both), `NotificationPreference` (one doc per user, channel booleans rather than one row per channel).

### `chat/`
`Conversation` (scoped to a `serviceRequest`, links `customer`+`technician`), `Message`.

### `reviews/`
`Review` — refs `serviceRequest`, `technician`; embeds `categoryRatings`.

### `technician/` (largest module — mirrors `ActiveJob.jsx`, the biggest frontend file)
- `Technician` — profile; embeds `skills[]`, `certifications[]`, `payoutMethods[]` (all small, always read with the profile).
- `Job` — **one per `ServiceRequest` (1:1)**, kept as its own collection rather than merged into `ServiceRequest` because most of its content (diagnosis, proofs, billing estimate, revisit sub-flow) only exists once a technician is actively engaged, and is always read/written together as a unit. Embeds `amc`/`ew` type-specific metadata, `diagnosis`, `proofs`, `revisit`, `billingEstimate`, and `additionalServices[]`/`spareParts[]` line items. `activeStep` covers both the main state machine (`idle → ... → completed`) and the revisit sub-flow states in one enum (`JOB_STEPS` + `JOB_REVISIT_STEPS` in `src/config/constants.js`).
- `TechInventoryItem` — `status` (In/Low/Out of Stock) is a **virtual**, derived from `qty`, never stored (can't drift out of sync).
- `PartOrder`, `EarningsTally` (one per technician, a running cache updated on `Payout` settlement), `Payout`.
- `TrainingGuide`, `Course`, `TechBlog`, `Announcement` (Academy content — no per-technician relation, read-only reference material).

### `brand-admin/` (every query here must be scoped by `brand` — enforced by `requireBrandScope` middleware, Phase 3)
`Invoice`, `RateCard` (feeds `pricingEngine`; `totalBase` is a virtual, not stored), `ReplacementApproval`, `ReverseLogisticsReturn`, the 3-level catalog hierarchy `MasterService` → `SubBrand` → `BrandProduct` (services mapped per product), `Team`, `GeneratedDocument`. **No separate `BrandUser` model** — a brand-admin account is just a `User` with `role: 'brand_admin'` and `brand` set; its permissions come from `Role` docs with `scope: 'brand'`.

### `super-admin/`
- Org/geo: `Brand`, `City` (no dependencies — referenced by nearly everything else), `ServicePartner` (refs `City`), `ASM` (refs `City` + `ServicePartner[]`).
- Ops: `AssignmentWeighting` (config for the Phase 8 auto-assignment scorer), `LiveTracking` (one doc per active `Job`, upserted on each GPS ping — latest position only, not history), `Escalation` (see "merge instead of duplicate"), `AuditLog` (append-only, no `humanId`/`toJSON` plugin needed), `SparePartCatalog` (`retailPrice`/`status` are virtuals), `PlatformSettings` (singleton).
- Finance: `BillingTransaction`, `Revenue` (aggregated rows, built by a reporting job — not per-transaction), `PartnerPayout` (payouts to a `ServicePartner`, distinct from technician-level `Payout`), `GatewayTransaction` (raw gateway log, distinct from the app-level `Payment`).
- CMS/app-customization: `Banner`, `Story`, `Video`, `Advertisement`, `CMSPage` (static pages by `slug`), `AppSetting` (flat key/value store per app — new toggles need no schema migration).

### `shared/`
`Counter` — backing store for `idGenerator` (Phase 2): one doc per prefix+scope key, atomically incremented via `findOneAndUpdate($inc)`.

## Indexes worth calling out

Every list-page filter pattern from `BACKEND_CONTEXT.md` §7.4 has a matching compound index: `ServiceRequest{brand,status,createdAt}` / `{technician,status}`, `Invoice{brand,status,createdAt}`, `Job{technician,activeStep}`, `Booking{user,status,createdAt}`, plus unique-scoped indexes (`Category.key`, `Coupon.code`, `Role{name,scope,brand}`, `RateCard{brand,category,serviceType}`) and a text index on `Product{name,brand}` for the search bar.

## Deviations from the original phase plan

- `Role` and `Escalation`: one collection with a `scope` discriminator instead of two separate collections (see Conventions above) — same tenant isolation, less duplication.
- `BrandUser` (mentioned in `BACKEND_CONTEXT.md` §5): folded into the single `User` collection rather than a separate model.

## Phase 2 addendum — shared services wired up

- **`idGenerator`** (`src/modules/shared/idGenerator.js`) is now live: `humanIdPlugin` (`src/modules/shared/plugins.js`) calls it from a `pre('save')` hook on every model configured with a `prefix`, atomically allocating the next ID via the `Counter` collection (`ID_SCHEMES` in `src/config/constants.js` defines digits/date-reset per prefix). **`User` is the one exception** — it doesn't use the generic plugin's prefix option, since a single `User` collection spans all 4 roles and only `role: 'customer'` should ever get a `CUST-###` id; it has its own small `pre('save')` hook gated on `role`.
- **Schema fix**: `ExtendedWarrantyOrder.appliance` was a free-text category string in Phase 1; it's now `{ appliance: ObjectId ref OwnedAppliance, applianceCategory: String }` so `warrantyEngine` can actually resolve the Extended Warranty overlay for a given appliance instead of only reading `AMCSubscription`.
- **`warrantyEngine`** and **`pricingEngine`** (`src/modules/shared/`) are pure functions (no DB access) — callers resolve `AMCSubscription`/`ExtendedWarrantyOrder`/`RateCard` docs and pass plain values in, which is what makes them cheaply unit-testable without a database.
- **`fileUpload`** (`src/modules/shared/fileUpload.js`): Multer buffers in memory; `storeUploadedFile` persists to local disk (`backend/uploads/`, gitignored) when `S3_*` env vars are unset, or to an S3-compatible bucket (`@aws-sdk/client-s3` — works against AWS S3 or Cloudflare R2) once they're configured. `app.js` only serves `/uploads` statically when the local backend is active.
- **Throwaway dev routes** (`src/modules/shared/dev.routes.js`, mounted at `/api/v1/_dev/*` only when `NODE_ENV !== 'production'`) exist solely to exercise pagination/validation/upload/id-generation end-to-end per this phase's exit criterion — covered by `e2e/api/dev.spec.js`. Expect both to be deleted once Phase 4+ real routes make them redundant.

## Phase 3 addendum — auth, sessions & RBAC wired up

- **Auth module** (`src/modules/auth/`): `POST /auth/{login,otp/send,otp/verify,forgot-password,reset-password,refresh,logout}`. Login is two-step, matching the frontend's existing UI flow — `login` verifies the password and sends an OTP (no tokens yet); `otp/verify` is what actually issues the JWT access+refresh pair. No signup endpoint exists yet — accounts come from `npm run seed` or an admin panel in a later phase.
- **JWT access tokens embed a `permissions` snapshot** (resolved from `User.assignedRoles -> Role.permissions -> Permission.key` at issuance time), so `requirePermission()` middleware never needs a DB hit — tradeoff is up to `JWT_ACCESS_EXPIRES_IN` (15m) of staleness if a role's permissions change mid-session.
- **Refresh tokens are rotated and hashed at rest**: `RefreshToken.tokenHash` (sha256), never the raw token; each `/auth/refresh` call revokes the old doc and issues a new one. Found and fixed a real bug here during testing — two refresh tokens signed for the same user within the same second are byte-identical JWTs (1s `iat` resolution) unless a nonce is added, which collided on `tokenHash`'s unique index; `signRefreshToken` now always includes a random `jti`.
- **`registerAllModels()`** (`src/config/registerModels.js`), called once at the top of `server.js`, imports every `*.model.js` so Mongoose has every schema registered before the first request — added after `.populate('assignedRoles')` threw "Schema hasn't been registered for model 'Role'" in manual testing, because nothing in the auth module's own import graph happened to load `role.model.js`/`permission.model.js`. This class of bug will keep recurring as more services add `.populate()` calls, so it's solved once here rather than per-service.
- **RBAC seed** (`backend/scripts/seed.js`, `npm run seed`, idempotent): 10 baseline permissions, a platform-scoped "Super Admin" role (all permissions) and a brand-scoped "Brand Admin" role (subset), one demo `Brand`, and exactly one `User` per role (customer/technician/brand_admin/super_admin) plus a linked `Technician` profile — reuses the frontend's demo personas ("Sakshi Dwivedi", "Rahul Sharma") and demo creds (`admin123@gmail.com` / `admin123` shared across both admin roles, matching what the frontend already hardcodes).
- **Auth rate limiting** (`authRouter`, 20 req/15min) is skipped under `NODE_ENV=test` — its in-memory store persists for the process lifetime, and a single Jest file legitimately exceeds 20 requests across its auth test cases; the limiter itself isn't what's under test there.
- **`User`'s compound unique indexes were silently broken since Phase 1** — `{phone:1,role:1}`/`{email:1,role:1}` were declared `sparse`, on the assumption a compound sparse index skips a document if *any* indexed field is missing. It doesn't: MongoDB only skips a document from a compound sparse index if it's missing *every* indexed field, and `role` is always present, so every user without a `phone` was still indexed as `phone: null` and collided with every *other* phone-less user of the same role. Only surfaced once the E2E suite created a second phone-less `super_admin` in a persisted database (`E11000 duplicate key`). Fixed with explicit `partialFilterExpression: { phone: { $exists: true } }` (and same for `email`) instead of `sparse`.
- **`ensureIndexes()`** (`src/config/db.js`), called after `connectDB()` in both `server.js` and `scripts/seed.js`: Mongoose's `autoIndex` fires `createIndexes` in the background and does *not* block on it, so a short-lived process — `seed.js` connects, writes, and disconnects in a couple hundred milliseconds — could exit before most of a model's indexes ever finished building, leaving uniqueness effectively unenforced. `ensureIndexes()` awaits `Model.init()` (which resolves once that model's indexes are confirmed built) for every registered model before any real work happens. This was found by directly inspecting `db.users.getIndexes()` after running `npm run seed` and seeing only 2 of 8 expected indexes present.

## Phase 4 addendum — catalog, booking & service-request routes wired up

- **Catalog module** (`src/modules/catalog/`): `GET /catalog/categories` and `GET /catalog/categories/:key` (public — the customer app browses without logging in) assemble `Category` + its `ProductType`s + `ServiceCatalogItem`s into exactly the shape `frontend/src/data/bookingCatalog.js`'s `BOOKING_CATALOG` entries already have (`{ key, productTypes: [{id,name,icon,desc}], services: [{id,name,icon,desc,price,unit}], ... }`), so no reshaping is needed once the frontend is wired to real APIs. Writes (`POST /catalog/categories`, `.../product-types`, `.../services`, `PUT /catalog/categories/:key`) are gated by `requireRole(SUPER_ADMIN)` for now — a real brand-scoped/permission-based CMS is Phase 8's job.
- **`backend/scripts/catalogSeedData.js`**: all 9 categories from `bookingCatalog.js` (AC, Washing Machine, Refrigerator, TV, RO Water Purifier, Geyser, Microwave, Chimney, Air Cooler), seeded by `scripts/seed.js` — same prices, product types, and services as the frontend mock, so the seeded backend catalog is a genuine drop-in replacement, not a token 1-category stub.
- **`findAvailableTechnician()`** (originally `src/modules/shared/assignmentStub.js`, since replaced — see the Phase 8 addendum) was the explicitly-temporary stand-in for Phase 8's full weighted auto-assignment engine: first `Active`+`Available` `Technician` whose `specs` includes the booking's category, falling back to any `Active`+`Available` technician. Same function signature Phase 8 filled in properly (`src/modules/shared/assignmentEngine.js`), so `booking.service.js` needed only a one-line change, not a rewrite.
- **`SERVICE_REQUEST_TRANSITIONS`** (`src/config/constants.js`): explicit allowed-next-status map, enforced server-side in `serviceRequest.service.js`'s `transitionStatus()` — the Phase 4 exit criterion ("status transitions validated server-side, not client-trusted"). `Closed`/`Cancelled` are terminal (no outgoing edges).
- **Booking creation is transactional in spirit but not yet in a Mongo `session`**: `createBooking()` prices server-side from the catalog (a client-supplied price is silently discarded — `createBookingSchema` doesn't even accept one), auto-assigns a technician, creates the linked `ServiceRequest`, and immediately transitions it `New -> Assigned` if a technician was found. Multi-document atomicity across `Booking`+`ServiceRequest`+`Counter` writes is deferred to Phase 5 (where the plan already calls for a real transaction on the payment/wallet path) rather than introduced piecemeal here.
- **Bug found via manual smoke testing, not just written to pass**: `booking.service.js` called `transitionStatus()` for the auto-assign step but discarded its return value, so the API response (and the `serviceRequest` the function returned) still showed status `"New"` with a 1-entry timeline even though the database was correctly updated to `"Assigned"`. Fixed by capturing and returning the updated document.
- **E2E test-isolation bug found via the E2E suite itself, not the (sequential, per-suite-DB) Jest tests**: booking/service-request E2E specs originally all booked against the shared seeded `'AC'` category. Under Playwright's parallel workers, `findAvailableTechnician`'s "first matching specialist" query nondeterministically picked whichever `Active`+`Available` technician with `'AC'` in `specs` happened to match first — sometimes the seeded "Rahul Sharma", sometimes another test's fixture technician, not necessarily the current test's own. Fixed by giving every booking/service-request E2E test its own uniquely-keyed category + service + technician (`setupIsolatedFixture()` in `e2e/api/booking.spec.js`), so no cross-test or cross-worker collision is even possible — not a retry/wait-based workaround.
- **`e2e/global-setup.js`** runs the real `backend/scripts/seed.js` (idempotent) against the e2e database once before the suite starts, reusing the exact same seed logic as local dev rather than a parallel fixture-creation path — `auth.spec.js` doesn't depend on it (creates its own users via `/_dev/test-user`), but `catalog.spec.js`'s read tests and the RBAC seed data do.
- **`POST /_dev/test-technician`** (new `NODE_ENV=test`-only dev route, same reasoning as `/_dev/test-user`): creates a `User`+`Technician` pair with caller-specified `specs`/`availability`, needed because booking/service-request E2E specs must control exactly which technician is eligible for auto-assignment.
- **Jest cross-file flakiness found and fixed — every test file now gets its own database, not one shared one**: 5 test files (`health`, `idGenerator`, `auth`, `catalog`, `booking`) each independently called `mongoose.connect()`/`dropDatabase()`/`disconnect()` against the *same* `nigam_care_test` database. Once there were enough of them, the full suite started failing intermittently (passed in isolation, occasionally failed as part of the full run) — one file's teardown could race against another's setup/tests on the same physical database. Fixed with `tests/helpers/testDb.js`'s `testDbUri(suffix)`, giving each file its own `nigam_care_test_<name>` database so this class of race is impossible by construction. Caught by running the full suite repeatedly (not just once) after the fix to confirm it actually resolved it, since the original failure didn't reproduce every time either.
- **Related bug in the same area**: `health.test.js` imports `createApp()`, which transitively imports *every* model (via the full route chain) even though the test only exercises `/health`. Mongoose's `autoIndex` schedules `createIndexes()` for each newly-registered model in the background, unawaited — those could still be in-flight when `afterAll`'s `dropDatabase()` ran, recreating empty collections with indexes immediately after the drop and leaving a stray database behind even on a supposedly-clean teardown. Fixed by adding the same `ensureIndexes()` call (`src/config/db.js`, already used by `auth`/`catalog`/`booking` tests) to `health.test.js`'s `beforeAll`, so all pending index builds resolve before the test runs and well before teardown.

## Phase 5 addendum — commerce, payments/wallet & exchange wired up

**Scope note, agreed with the user up front:** this phase covers the core commerce path the exit criterion needs — `Product`/`Cart`/`Wishlist`/`Coupon`/`Exchange`/`Order`/`Wallet`+`Payment` — with the same rigor (smoke-tested, Jest, E2E) as every prior phase. `ExtendedWarrantyOrder`/`AMCPlan`/`AMCSubscription`/`AMCVisit` (the AMC/Extended-Warranty *purchase* flow), `Referral`, `Membership`, `SpinWheelConfig`, and `LoyaltyMilestone` are **deliberately deferred**, not silently dropped — cramming 6+ more sub-domains into one phase risked diluting quality across all of them. These models already exist (Phase 1); only their service/routes layers remain, to be picked up in a dedicated follow-up.

- **No Mongo transactions — a real, discussed tradeoff, not an oversight.** The plan's original Phase 5 called for a multi-document Mongo transaction wrapping payment + wallet debit + order/booking status. That requires the mongod to run as a replica set; the local dev instance is standalone, and the user explicitly chose *not* to convert it (a system-wide, out-of-repo change) rather than build around an assumption. Instead:
  - `wallet.service.js`'s `redeemCoins`/`creditCoins` and `product.service.js`'s `decrementStock` are atomic at the **single-document** level via `findOneAndUpdate` with a `{ $gte: amount }` guard in the filter — MongoDB serializes writes to the same document, so concurrent debits that would together overdraw a balance genuinely cannot both succeed. This is what the wallet concurrency test (`tests/wallet.test.js`) verifies: 5 parallel requests for 150 coins each against a 500-coin balance — exactly 3 succeed, 2 fail cleanly, final balance is exact.
  - What this does **not** provide: atomicity *across* documents. `order.service.js`'s `createOrder()` is sequential (coins redeemed → stock decremented → payment created → order created) with **best-effort compensation** in a `catch` block (stock/coins credited back) if a later step fails — not a guaranteed all-or-nothing unit. A crash between steps (not just a thrown error) would leave an inconsistent state. Revisit once Atlas (or a local replica set) is available — Phase 12's deployment plan already lands on Atlas, which gives this for free.
  - **Update:** `backend/.env`'s dev `MONGODB_URI` now points at a MongoDB Atlas cluster (moved up from Phase 12) — Atlas *does* give a replica set, so real transactions are now technically possible in dev. **Deliberately not adopted yet, by explicit choice**: Jest/E2E still run against local standalone `mongod` (fast, offline-friendly, no shared-cluster contention from many parallel test databases), and service code that called `session.startTransaction()` would hard-fail there (`"Transaction numbers are only allowed on a replica set member or mongos"`). Converting would mean either moving the whole test suite to Atlas too, or feature-detecting replica-set support at runtime — neither undertaken yet. So `wallet.service.js`/`order.service.js`/`job.service.js`'s payment-collection path all still use the single-document-atomic + best-effort-compensation pattern described above, unchanged, even against Atlas.
- **`Order` gained a pricing-breakdown block** (`subtotal`, `couponCode`, `couponDiscount`, `exchangeDiscount`, `coinsValue`, `total`) beyond Phase 1's bare `total` — every figure computed server-side in `order.service.js`, never trusted from the client (the request schema doesn't even accept a price/total field).
- **Coin redemption is capped at what's actually owed**, not the client's full requested amount — `coinsToRedeem` requests more value than the remaining payable amount, only the needed coins are debited (verified in both `commerce.test.js` and the concurrency-adjacent capping test). Coupon and exchange discounts are similarly clamped so a combination can never push `total` negative.
- **`Payment.targetId` is `required`, which meant it couldn't be created before the `Order` it targets exists** — caught before ever running the code, while re-reading it during the Bash-unavailability window. Fixed by pre-generating the `Order`'s `_id` (`new mongoose.Types.ObjectId()`) so `Payment` and `Order` can cross-reference each other from creation, avoiding a two-phase create-then-update.
- **Exchange valuation** (`exchangeValuation.js`) is a pure function — `estimatedValue = baseValue - deductionsAmount + bonusAmount`, floored at 0 — exactly `frontend/src/data/exchangeMockData.js`'s formula. `POST /exchange/valuate` quotes without persisting (lets the frontend show a live estimate as questions are answered); `POST /exchange/requests` persists the *computed* valuation onto the `ExchangeRequest`, and `order.service.js` always reads `estimatedValue` from that stored document — a later question-set edit can never retroactively change an already-quoted trade-in value. An `ExchangeRequest` can only be applied to one order (`markApplied()` rejects reuse).
- **New admin-editable write added beyond the original plan**: `POST /exchange/question-sets` — without it, E2E specs would have had to share the one seeded `'Mobile'` question set, reintroducing the exact cross-test category-collision class of bug fixed in Phase 4. Same `requireRole(SUPER_ADMIN)` gate as catalog/product writes.
- **`POST /_dev/test-user` now accepts an optional `walletCoins`** starting balance (NODE_ENV=test only) — needed because there's still no self-serve signup/credit endpoint, and E2E specs testing coin redemption need a real starting balance to redeem from.
- **`paymentGateway.js`** is a stub behind the same kind of interface as Phase 3's `otpProvider.js` — every charge "succeeds" with a synthetic `gatewayRef`. Real Razorpay integration (inferred from the super-admin settings field `razorpayKey` — BACKEND_CONTEXT.md §9 open question) plugs in later without `order.service.js` changing.
- **Coupon `discount` is a flat rupee amount, not a percentage** — matches the frontend's `Coupons.jsx` badge copy, and is the simpler of two plausible readings of the Phase 1 schema comment; flagging in case a future phase's frontend integration reveals percentage-based coupons were actually intended.

## Phase 6 addendum — technician domain wired up

`Technician` profile + payout methods, `Job` (the technician-facing state machine), tech-side `Claim`, `TechInventoryItem`(read)/`PartOrder`, `EarningsTally`/`Payout`, and read-only Academy/`Announcement` routes, all mounted under `/api/v1/tech/*`.

- **Two state machines kept in lockstep, not one.** `Job.activeStep` (`JOB_STEP_TRANSITIONS`) and `ServiceRequest.status` (`SERVICE_REQUEST_TRANSITIONS`) are separate collections/enums — brand-admin and customer screens read the `ServiceRequest`, not the `Job` — so every technician action in `job.service.js` that advances `activeStep` also calls `transitionStatus()` on the linked `ServiceRequest` to the matching status: `acceptJob` → `Engineer Accepted`, `startTravel` → `Visit Scheduled`, `arrive` → `Engineer Reached`, `submitDiagnosis` → `Diagnosis Done`, `submitSpareParts` (only if at least one part is checked) → `Spare Required` → `Spare Ordered` → `Spare Received` (modeled as an immediate pass-through — no real "waiting for parts to arrive" delay window), `confirmRepairComplete` → `Repair Completed`, `collectPayment` → `Customer Confirmation`. `transitionStatus()` itself throws on an illegal move, which is treated as a real bug signal here rather than swallowed — the two machines are never expected to drift.
- **Bug found before ever exercising the code, while wiring the second transition point**: the first draft called `transitionStatus(..., 'Repair Completed')` from `collectPayment()` too — but `confirmRepairComplete()` already puts the `ServiceRequest` at `'Repair Completed'` by that point, so the real next legal move from `collectPayment` is `'Customer Confirmation'`. Caught by re-reading `SERVICE_REQUEST_TRANSITIONS` against the actual call sequence before running anything, not by a failing test.
- **AMC/Extended-Warranty *purchase* flows are still deferred (Phase 5's scope decision stands)**, so there's no real way for a `Job` to know it's covering an existing `AMCSubscription`/`ExtendedWarrantyOrder` other than being told. `acceptJob(technicianId, serviceRequestId, { type, amcSubscriptionId, extendedWarrantyOrderId })` accepts these as explicit, caller-supplied overrides — real `'NCC Paid Service'` jobs (via `Booking`) always resolve `type` automatically; anything else needs an explicit type + linkage, which in a fuller build would come from the brand/AMC context instead. `backend/scripts/seed.js` now seeds one `AMCSubscription` (LG, Gold, 4 visits) and one `ExtendedWarrantyOrder` (Voltas, 3 claims) against the demo customer so this path has something real to test against locally.
- **Warranty/AMC-covered jobs never bill the customer for spare parts.** `submitSpareParts()` checks `job.isD2C`: for D2C jobs, checked parts go on the bill as normal; for anything else, each checked part instead auto-raises a technician `Claim` (`raisedByModel: 'Technician'`, `claimType`/`brand` mapped per job `type` — `'AMC Visit'` → `Warehouse Order`/`NCC Warehouse Order`, `'NCC Extended Warranty'` → `Extended Warranty`/`NCC EW Claim`, `'Brand Warranty'` → `Brand`/`Brand Warranty Claim`) via the same `claim.service.js` the manual `POST /tech/claims` endpoint uses — `generateBilling()` then prices `sparePartsTotal` as 0 for these jobs.
- **Technician earnings on covered jobs are a flat placeholder (₹150/visit), not a computed rate** — there's no brand `RateCard` yet (that's Phase 7 scope) to price a technician's visit fee on warranty/AMC/EW work. `acceptJob()` sets `estEarnings` to this flat amount for any non-D2C job type (D2C keeps the existing 30%-of-subtotal share); `generateBilling()` carries it through to `billingEstimate.technicianEarnings` unchanged. Caught during manual smoke testing — the first AMC job completed with the customer's real total correctly at ₹0 (covered), but also credited the technician ₹0, which is a real gap, not intended behavior; `FLAT_COVERED_VISIT_EARNINGS` in `job.service.js` documents this as a placeholder to replace once RateCards exist. **User confirmed (post-Phase-6): keep the flat placeholder as-is** — not urgent enough to special-case ahead of Phase 7's real RateCard work.
- **`collectPayment()` on an AMC Visit job decrements `AMCSubscription.visitsRemaining` and `visitNumber`** via the same atomic `findOneAndUpdate` + guard pattern as `wallet.service.js` (`{ visitsRemaining: { $gt: 0 } }`), and upserts a matching `AMCVisit` to `status: 'Completed'`. An Extended Warranty job instead decrements `ExtendedWarrantyOrder.claimsRemaining`. Neither is wrapped in a transaction with the `Payment`/`EarningsTally` writes — same accepted, documented tradeoff as Phase 5 (no local replica set; the user explicitly declined converting the local mongod).
- **`requestPayout()` follows the same riskiest-first-plus-best-effort-compensation shape as `order.service.js`'s `createOrder()`**: debits `EarningsTally.total` atomically first (`{ total: { $gte: amount } }` guard — the same pattern proven under concurrency by `tests/wallet.test.js`), then creates the `Payout`; if the `Payout.create()` step itself fails, the tally debit is compensated back in a `catch`, not left stranded.
- **`attachTechnician` middleware** (`src/middleware/technician.js`) resolves `req.user` (the JWT subject, a `User`) to its `Technician` profile and 404s if none exists — every route under `/api/v1/tech/*` is mounted behind `requireAuth, requireRole(ROLES.TECHNICIAN), attachTechnician`, so handlers always work with a real `Technician._id`, never a raw `User._id`.
- **New `NODE_ENV=test`-only dev fixture routes** (`dev.routes.js`, same reasoning as `/_dev/test-technician`): `POST /_dev/test-amc-subscription` + `GET /_dev/amc-subscription/:id` and `POST /_dev/test-service-request` — AMC subscriptions and non-booking `ServiceRequest`s (e.g. an AMC-visit complaint raised directly, not via a `Booking`) have no real HTTP surface yet, so Playwright specs need a way to get real linkable documents without building those deferred flows early.
- **`Payment.targetType` enum extended** with `'job'` (previously `['booking', 'order', 'extended_warranty', 'amc']`) — a `Job`'s payment collection creates a `Payment` the same way `order.service.js` does, just targeting the `Job._id`.

## Phase 7 addendum — brand-admin domain wired up

`Invoice`, `RateCard`, `ReplacementApproval`, `ReverseLogisticsReturn`, the 3-level brand catalog hierarchy (`MasterService` → `SubBrand` → `BrandProduct`), `Team`, brand-scoped `Role`/`BrandUser`, `GeneratedDocument`, all mounted under `/api/v1/brand/*` behind `requireAuth, requireBrandScope`. All eight Phase 1 models already existed; this phase was entirely service+routes.

- **Closed a cross-tenant gap left open since Phase 4**: `service-requests/serviceRequest.routes.js`'s `GET /`, `GET /:id`, and `PATCH /:id/status` had a comment saying "brand-scoping lands in Phase 7" — until now, ANY `brand_admin` could list, view, and **transition the status of any ServiceRequest system-wide**, not just their own brand's. Fixed by scoping `GET /` to `req.user.brand` and adding a brand-match check to both `GET /:id` and `PATCH /:id/status`. `listServiceRequests()` gained a `brand` filter to support this.
- **Real regression caught by the Jest suite, not written correctly the first time**: the first draft of that fix collapsed the list/get authorization check (`canView`) and the status-transition check into one shared function. That's wrong — a *customer* can view their own ServiceRequest but must never be able to transition its status (only the assigned technician or an admin can). Reusing `canView` for the PATCH route accidentally let a customer drive their own request's status. Caught immediately by `booking.test.js`'s pre-existing "rejects a transition attempt from the customer with 403" test going red on a full-suite run. Fixed by splitting into two functions — `canView` (broader) and `canTransition` (narrower, no customer branch) — with a comment explaining why they must stay separate.
- **Real bug caught during manual smoke testing, before any automated test existed**: `brandUser.service.js`'s `inviteBrandUser()` originally returned the `User.create()` result directly. `passwordHash` has `select: false` on the schema, but that only suppresses the field on subsequent *queries* — a freshly created document handed back by `.create()` still has it in memory, so the `POST /brand/users` response leaked the bcrypt hash. Fixed by re-fetching via `User.findById()` (which does respect `select: false`) before returning. `tests/brandAdmin.test.js` and `e2e/api/brandAdmin.spec.js` both assert no bcrypt-hash-shaped string (`/\$2[aby]\$/`) ever appears anywhere in the response body, not just that a `passwordHash` key is absent.
- **Real bug in `team.routes.js`'s `DELETE /:id/members/:userId`, caught by a Jest test actually removing nothing**: it validated `req.params` against `idParamSchema` (`{ id }` only). `validate()`'s `req[source] = result.data` replaces the whole params object with Zod's parsed output — and Zod strips unknown keys by default — so `userId` was silently deleted from `req.params` before the handler ever ran, making `removeMember(brandId, id, undefined)` a no-op filter. Fixed with a dedicated `memberParamSchema` covering both `id` and `userId`, plus a comment on the general hazard: **any `validate(schema, 'params')` call must list every dynamic segment the route pattern declares**, not just the ones the handler happens to read first.
- **`inviteBrandUser` has no real email-invite infrastructure** (same gap as OTP/SMS, BACKEND_CONTEXT.md §9) — it creates the account directly with an admin-supplied initial password, matching how `scripts/seed.js` creates every other role's account today.
- **`replacementApproval`/`reverseLogisticsReturn` both double as their own creation entry point**, not just an approve/reject queue — there's no technician-side "request a replacement" or "log a returned part" trigger yet (out of Phase 6's scope), so brand-admin/ops logs these manually for now. Revisit once that technician-side flow exists.
- **Most real `ServiceRequest`-creation paths still don't set a real `Brand` tenant ref**: `booking.service.js`'s D2C flow never sets `ServiceRequest.brand` (a D2C repair isn't tied to any manufacturer's brand-admin dashboard — correct behavior), and Phase 6's AMC/Extended-Warranty job-acceptance path doesn't either (the AMC/EW purchase flow itself is still deferred, and `AMCSubscription.brand`/`ExtendedWarrantyOrder.brand` are free-text manufacturer labels, not `Brand` ObjectId refs — a real, separate modeling gap between "Brand the platform tenant" and "brand the appliance manufacturer name" that this phase did not attempt to reconcile). In practice, today nothing in production code sets `ServiceRequest.brand`; it's exercised here only via new `NODE_ENV=test`-only fixtures.
- **New `NODE_ENV=test`-only dev fixture routes** (`dev.routes.js`): `POST /_dev/test-brand` (`Brand` has no real CRUD yet — that's Phase 8) and `/_dev/test-user` gained an optional `brand` field (brand_admin-only) and `/_dev/test-service-request` gained an optional `brand` field — together these let Playwright specs construct two real, separate tenants to prove cross-tenant isolation against, without building the deferred purchase/complaint flows early.
- **`scripts/seed.js` now seeds a second brand** ("Second Demo Brand") with its own Brand Admin role and admin user (`admin2@gmail.com` / `admin123`) — purely for manual smoke testing and the Postman collection; the automated Jest/E2E suites build their own throwaway two-brand fixtures per test, same isolation reasoning as every other "asserts on specifics" test in this codebase.
- **`RateCard` writes are upsert-on-(brand,category,serviceType)**, not create-then-conflict — matches `CallRatesCharges.jsx`'s "set the rate for this category/service" UX and avoids ever hitting the model's unique index as an error path.
- **Brand-scoped `Role` creation resolves permission keys to `Permission` ObjectIds server-side** (`POST /brand/roles` accepts `permissionKeys: string[]`, e.g. `['requests:manage']`) and 400s on any unknown key — never trusts client-supplied `Permission` ObjectIds directly. `updateBrandUser`/`inviteBrandUser` similarly validate that any `assignedRoles` actually belong to the caller's own brand (`scope: 'brand', brand: req.user.brand`) before assigning them, closing an otherwise-obvious cross-tenant privilege-escalation path (assigning yourself a role that belongs to another brand).

## Phase 8 addendum — super-admin domain wired up

`Brand`/`City`/`ServicePartner`/`ASM`, the real weighted auto-assignment engine, `LiveTracking`, `Escalation` (platform-scope), `AuditLog`, `SparePartCatalog`, `PlatformSettings`, CMS/app-customization, loyalty config authoring, and platform-wide RBAC + cross-role user management — all under `/api/v1/super-admin/*` (super_admin-only) except `/api/v1/cms/*` (public reads, admin-gated writes).

- **Scope note, agreed up front (same reasoning as Phase 5's AMC/EW deferral)**: the Finance sub-module (`BillingTransaction`, `Revenue`, `PartnerPayout`, `GatewayTransaction`) is **deliberately deferred**, not silently dropped. All four models exist from Phase 1, but none of them have a real system-generated data source anywhere in the codebase: no commission-calculation job populates `Revenue`, no partner-earnings ledger exists to compute a `PartnerPayout.balance` from, no payment-gateway webhook integration exists to log `GatewayTransaction` rows, and nothing creates a `BillingTransaction` as a side effect of any existing flow. Building bare CRUD shells with manual-entry-only endpoints for four reporting/reconciliation collections with nothing real to report would be hollow scaffolding — better spent making the rest of this large phase (10 other sub-modules) solid and well-tested. Revisit alongside real payment-gateway integration (`paymentGateway.js`'s stub, BACKEND_CONTEXT.md §9) and a partner-commission design.
- **Real weighted auto-assignment engine** (`src/modules/shared/assignmentEngine.js`) replaces the Phase 4 stub (`assignmentStub.js`, deleted) but keeps the same call shape (`{ category }`) plus one added optional field (`{ category, city }`) — `booking.service.js` only needed a one-line change (pass `city: data.address?.city`), not a rewrite, matching the Phase 4 addendum's stated intent. Scores every `Active`+`Available` `Technician` (hard-filtered — unavailable technicians can never win regardless of score) on four weighted factors read from the admin-configurable `AssignmentWeighting` singleton (`PUT /super-admin/assignment-weighting`, 400s unless the four percentages sum to exactly 100): proximity (city-name string match against the technician's `City` doc — a simplified proxy, since neither `Technician` nor `Booking` store real lat/lng yet), skill (100 if the category is in `specs`, else 40 — not a hard filter, so a generalist is still assignable, just disadvantaged), rating (`0–5` scaled to `0–100`), workload (`100 − activeJobsCount × 20`, floored at 0). Verified with 9 direct unit-style tests (`tests/assignmentEngine.test.js`) covering the hard filter, the fallback-to-best-generalist case, tie-breaking, city-match proximity, and that a strong enough rating advantage genuinely can outweigh a specs mismatch (proving the scoring is real weighting, not skill-gated filtering with cosmetic extra factors).
- **`e2e/api/booking.spec.js`'s existing per-test-unique-category isolation pattern still holds under the new engine** — re-derived and verified by a dedicated unit test (`tests/assignmentEngine.test.js`'s "fresh fixtures" case): with no `city` passed (proximity ties at 50 for every candidate) and every fixture technician starting at `rating: 0, activeJobsCount: 0`, the 60-point skill-score gap (100 vs 40) is the only real differentiator, worth 18 weighted points under the default 40/30/20/10 split — enough to beat any other technician regardless of what other concurrent tests are doing to their own fixtures' workload.
- **Real bug caught during manual smoke testing, not written to pass**: `PlatformSettings`, `CMSPage`, and `AppSetting` models never called `applyStandardPlugins`, so their API responses leaked raw Mongo `_id`/`__v` instead of the `id` shape every other endpoint in this codebase returns. `AuditLog`'s model had a comment explicitly deciding against the plugin ("no toJSON/humanId plugin needed for an internal log") — true when it was write-only, no longer true once this phase gave it a real `GET /super-admin/audit-logs` surface. All four models fixed to call `applyStandardPlugins()` (no `prefix`, so no `humanId` — just the `_id → id` / drop-`__v` shaping).
- **Real bug found and fixed while running the full E2E suite for the first time this phase**: two `technician.spec.js` tests started failing with a raw `"Too many requests"` string body (not JSON) instead of their real assertions. Root cause: the **global** rate limiter in `app.js` (`limit: 600` per 15 minutes) was never skipped in `NODE_ENV=test`, unlike `auth.routes.js`'s stricter `authRateLimit`, which already was. The E2E suite's total request volume across now-15 spec files sharing one long-lived server process (`webServer.reuseExistingServer`) legitimately crossed 600 requests within a single test run once Phase 8 added ~25 more tests — not a real signal to test against, just an artifact of how Playwright's `webServer` is configured. Fixed by wrapping the global limiter in `if (!isTest)`, matching the existing `authRateLimit` precedent exactly.
- **CMS content types (`Banner`/`Story`/`Video`/`Advertisement`/`CMSPage`/`AppSetting`) are public-read, admin-write** — same pattern as the Phase 4 catalog module (`GET` routes have no `requireAuth`, `POST`/`PUT`/`DELETE` do), consumed by the customer/technician apps. This is literally what `CustomerAppCustomization.jsx` needs to be a real merchandising builder for, per the original roadmap note.
- **The Phase 8 exit criterion ("super-admin can edit a category via CMS endpoints and the Phase 4 catalog endpoint immediately reflects it, no redeploy") is satisfied by the *existing* Phase 4 catalog module**, not a new CMS-specific catalog endpoint — `catalog.routes.js`'s `PUT /catalog/categories/:key` was already gated by `requireRole(SUPER_ADMIN)` since Phase 4, and reads from the same collection every customer-facing `GET` hits. Verified live (manual smoke test) and with a dedicated Jest + E2E test asserting the read reflects the write with no intermediate step.
- **Loyalty config authoring is the super-admin CRUD layer over `LoyaltyMilestone`/`Membership`/`SpinWheelConfig`** (models live in `rewards-loyalty/`, routes live in `super-admin/loyaltyConfig.routes.js`) — explicitly **not** the customer-facing earn/redeem/spin/referral *flows*, which stay deferred per the Phase 5 scope decision the user reaffirmed post-Phase-6. `coin_rate` and referral bonus amount have no dedicated collection (they're single platform-wide numbers, not rows) — added as `PlatformSettings.coinConversionRate`/`referralBonusAmount` instead of a one-row collection of their own. Spin-wheel segment probabilities are validated to sum to ≤100 server-side, exactly what the model's own Phase 1 comment deferred to "the super-admin service layer in Phase 8."
- **`Escalation` routes only ever touch `scope: 'platform'` documents** — `scope: 'brand'` rows have no route anywhere yet (brand-admin's own `Escalations.jsx` surface wasn't in Phase 7's shipped list either). A real, documented gap, not an oversight; `escalation.service.js` 403s if a platform-scope helper is ever pointed at a brand-scope id.
- **`LiveTracking` write access stayed super-admin-only** rather than also letting a technician PUT their own job's location — the real write path (a technician's live GPS ping) is explicitly Phase 9's Socket.IO handler, not an HTTP route; opening a technician-writable HTTP endpoint now would have needed its own ownership-check design for a code path that's getting replaced in the very next phase.
- **`logAudit()` (`src/modules/shared/auditLog.js`) is wired into two call sites only** (`PlatformSettings` updates, `Brand` create/status-change) as a proof of concept, not retrofitted across every mutating action in the app — a comprehensive audit trail is a larger, cross-cutting effort explicitly out of this phase's scope.
- **`scripts/seed.js` gained a `City` ("Lucknow"), a `ServicePartner` in it, and an explicit `AssignmentWeighting` doc** — the seeded technician is now linked to that city/partner so the real assignment engine has real proximity data to score against locally, not just an unlinked technician relying on the 50-point "unknown" default.

## Phase 9 addendum — real-time layer wired up

Socket.IO attached to the HTTP server (`src/sockets/`), `Conversation`/`Message` chat (`src/modules/chat/`), and `Notification`/`NotificationPreference` (`src/modules/notifications/`) wired as real domain-event side effects rather than ad-hoc writes.

- **`server.js` now wraps the Express app in a raw `http.Server`** (`http.createServer(app)`) instead of calling `app.listen()` directly, so `src/sockets/index.js`'s `initSockets(httpServer)` can attach a `Server` from `socket.io` to the same port. `createApp()` itself is unchanged — Jest's existing `supertest(createApp())` pattern across every prior test file keeps working untouched.
- **Room-per-conversation is what makes the exit criterion a structural guarantee, not an application-level filter.** `chat.gateway.js`'s `join-conversation`/`send-message` re-check participant membership (customer or the *specific* technician on that `Conversation`) on every call, not just once at join time, then `io.to('conversation:<id>').emit(...)` — a socket that never joined that room cannot receive the emit, full stop, regardless of what event name or payload shape a client tries. Verified directly in `tests/sockets.test.js`: two customers in two different conversations, one sends, the other's client receives literally nothing (a timeout-based wait, not just "no error").
- **No public "create conversation" endpoint.** `getOrCreateConversation()` is called as a side effect of `technician/job.service.js`'s `acceptJob()` — the one moment customer+technician+serviceRequest are already a verified real pairing (the technician was actually assigned to that specific `ServiceRequest`). A client-facing `POST /chat/conversations` accepting arbitrary `customer`/`technician` ids would let any technician start a "conversation" with any customer with no real relationship — not built.
- **`tracking.gateway.js`'s `update-location` is the real write path `liveTracking.routes.js` (Phase 8) predicted** — reuses the exact same `upsertTracking()` service function the Phase 8 HTTP `PUT` route calls, so both write paths stay consistent. A technician can only update the job actually assigned to them (`Job.technician` match); only `super_admin` sockets may `join-tracking`.
- **`notification.service.js`'s `emit()` never throws** — an unknown event or a template/DB failure is caught, logged via `console.error`, and returns `null`. This is deliberate: a booking, job-payment-collection, or escalation-creation action must never fail because its *confirmation notification* had a bug. Every call site (`booking.service.js`, `job.service.js`, `escalation.service.js`, `claim.service.js`) just `await`s it for ordering, no try/catch needed at the call site.
- **`claim.approved` needed a real trigger point that didn't exist**: no claim-approval endpoint existed anywhere before this phase (`Claim.status` had the enum but nothing transitioned it). Added a minimal `super-admin/claim.routes.js` (`GET /super-admin/claims`, `PATCH /:id/status`) as a stand-in authority — same reasoning as the Phase 7 addendum's documented gap (`Claim.brand` is a free-text label, not a `Brand` ObjectId ref, so a real brand-scoped approve/reject surface isn't cleanly buildable yet). Resolves the raiser to a real `User` id for the notification even when `raisedByModel: 'Technician'` (looks up `Technician.user`).
- **Real bug found during manual smoke testing, before any automated test existed**: `notification.validation.js`'s `read` query param used `z.coerce.boolean()`, which coerces via JS's `Boolean(str)` — `Boolean("false")` is `true` (any non-empty string is truthy) — so `GET /notifications?read=false` silently returned *read* notifications instead of unread ones, the filter's result was inverted. Fixed with `z.enum(['true','false']).transform(v => v === 'true')`, which parses the literal string value instead of relying on JS's truthy-string coercion. `tests/notifications.test.js` has a dedicated regression test asserting both directions of the filter. Worth flagging generally: `z.coerce.boolean()` is almost never what you want for a query-string boolean — grep confirmed this was the only place in the codebase using it.
- **Broadcast notifications (`recipient: null`, `broadcastRole` set) have no per-recipient read state** — a single shared `read` boolean on the document, not a read-receipt row per user. `markRead()` 400s if pointed at a broadcast notification (would otherwise mark it read for every recipient at once, the first user to open it silently hiding it from everyone else); `markAllRead()` only ever touches personally-addressed notifications. A documented schema limitation carried over from Phase 1, not something this phase attempted to redesign — a real fix needs a `NotificationRead { user, notification }` join collection.
- **Phone masking covers the chat REST surface only** — `conversation.service.js` masks both participants' phone numbers (`otpProvider.js`'s existing `maskIdentifier`) in every response. Real masked *calling* (a telephony relay so neither party ever sees the other's raw number even on a live call) needs an actual telephony provider integration, same class of gap as the OTP/SMS provider (BACKEND_CONTEXT.md §9) — not attempted here.
- **`socket.io-client` added as a devDependency** — the only realistic way to write a real assertion for "two connected Socket.IO clients" is to actually connect two Socket.IO clients; `tests/sockets.test.js` spins up a real `http.Server` (not just `supertest`'s in-process wrapper) on an ephemeral port per test run.

## Phase 10 addendum — testing & QA hardening

- **Real gap found and closed**: the `reviews` module had a `Review` model since Phase 1 but no service/routes — nobody had built it across Phases 6/7 despite the README saying "Phase 6/7". Filling it (`review.service.js`/`review.routes.js`) wasn't optional busywork; it's what made a genuine end-to-end "customer books → technician completes → payment → review" test possible at all, which is this phase's own exit criterion. One review per `ServiceRequest` (`409` on a second attempt), created only by the request's owning customer, publicly readable per-technician, respondable by the owning brand (cross-tenant-checked the same way `invoice.service.js` is). `tip` is captured, not settled — no tip-payout flow exists, flagged rather than half-built.
- **`tests/fullJourney.test.js`** is the true end-to-end flow the roadmap asked for: real HTTP calls through catalog browse → booking (real weighted assignment engine picks the technician) → job acceptance (auto-creates the chat conversation) → the full job lifecycle → payment collection → the resulting notifications → a review → the review appearing on the technician's public list → a rejected duplicate review. One continuous test exercising eight modules built across Phases 1–10 in the order a real user would hit them, not eight separate module-scoped tests stitched together after the fact.
- **`get_knowledge_gaps_tool` (code-review-graph) audited for untested backend hotspots** beyond what module-level test files already cover — found none beyond `validate`/`ok`, both trivial, high-degree utilities already exercised transitively by literally every other route test in the suite (adding dedicated unit tests for them would be padding, not real coverage).
- **`jest.config.js` gained `testTimeout: 20000`** (previously the 5000ms default) — a real, repeatedly-observed flake class where a `beforeAll` hook (fresh Mongoose connection + `ensureIndexes()` awaiting every model's index build) intermittently exceeded 5s purely from local mongod contention under `--runInBand` with the suite now at 20+ files, not a real hang (the same hooks always finish well under 20s in isolation or on a clean run). A genuine deadlock/bug still fails — this just gives legitimate load-induced slowness realistic headroom instead of flaking the whole run.

## Phase 11 addendum — security hardening

Ran the project's `/security-review` skill (3-step research → per-finding filtering-subagent → confidence≥8 gate) against the full `backend/` diff. Two genuine, exploitable, server-side vulnerabilities confirmed and fixed; several plausible-sounding candidates investigated and correctly filtered out as non-issues or below the confidence bar.

**Fixed — Vuln 1: cross-customer IDOR on AMC/Extended-Warranty job linkage.** `technician/job.service.js`'s `acceptJob()` accepted a caller-supplied `amcSubscriptionId` / `extendedWarrantyOrderId` and linked it onto the job with no ownership check — a technician could supply *any* `AMCSubscription`/`ExtendedWarrantyOrder` id, including one belonging to a completely different customer than the `ServiceRequest` they're servicing, and it would silently attach, later decrementing that other customer's visit/claim balance on `collectPayment()`. Fixed by checking `String(subscription.user) === String(serviceRequest.user)` (and the equivalent for the EW order) before linking, `403`ing otherwise. Regression tests: `tests/technicianJob.test.js` — "rejects linking an AMC subscription that belongs to a different customer..." and the equivalent EW test, both asserting `403` and that no `Job` document was created.

**Fixed — Vuln 2: unverified trade-in valuation usable as a free order discount.** `ExchangeRequest.estimatedValue` is entirely self-reported by the customer at valuation time (`answers` to a question set they also control the honesty of), yet `buy-commerce/order.service.js`'s `createOrder()` applied it directly as a checkout discount, checking only `appliedToOrder` (reuse) — never `status`. The model already had a `status` enum (`'Pending Inspection' | 'Inspection Approved' | 'Defective Received' | 'Received at WH'`) implying a real inspection gate was intended, but grepping the entire `src/modules/` tree turned up **no code anywhere that ever transitioned it out of its default `'Pending Inspection'`** — the gate existed in the schema but was never wired to anything. A naive fix (just add the status check) would have made the exchange-discount feature permanently unusable, since nothing could ever satisfy it. Built the missing piece as part of the fix:
  - New admin surface `super-admin/exchangeRequest.routes.js` (`GET /`, `GET /:id`, `PATCH /:id/status`), backed by new functions in `exchange.service.js` (`listExchangeRequestsAdmin`, `getExchangeRequestAdmin`, `updateExchangeRequestStatus`) — same shape as Phase 9's `super-admin/claim.routes.js`, gated `requireRole(ROLES.SUPER_ADMIN)`. Lets an admin mark a trade-in `'Inspection Approved'` after physically receiving/verifying the device.
  - `order.service.js`'s `createOrder()` now rejects (`400`) applying an exchange discount unless `exchangeRequest.status === 'Inspection Approved'`.
  - This is a genuine, deliberate scope addition beyond "add a check" — it's the minimum real workflow needed for the security fix to not break the feature it's protecting. **Open product question for the user**: today *any* `super_admin` can approve *any* trade-in platform-wide (mirrors the same "platform-wide claim approval, no real per-brand authority yet" gap already documented in the Phase 7/9 addenda for `Claim`). If trade-in inspection should instead be a warehouse-role or brand-scoped responsibility, that's a real product decision, not something inferable from the existing code — flagged in the final summary, not decided unilaterally here.
  - Regression tests: `tests/commerce.test.js` ("rejects an exchange discount for a request that has not been approved..."), `tests/superAdmin.test.js` (new `describe('Exchange requests — physical inspection workflow...')`, covering list/get/approve plus a 403 for non-super-admins), and `e2e/api/commerce.spec.js` (updated the two existing exchange-discount E2E tests to approve first via the new endpoint, plus a new test asserting the discount is rejected pre-approval and the approval endpoint itself is 403 for a customer).

**Investigated and correctly filtered out (not fixed, not findings):**
- A bank account number visible in a payout-method response, but only ever to its own owning technician (`req.user.id` scoped) — not cross-user-visible, so not an IDOR.
- Dev-only endpoints (`shared/dev.routes.js`) gated behind `!isProd` (trusted `NODE_ENV` config, not user input) — consistent with this project's established "environment variables are trusted values" precedent.
- OTP code randomness (`otpProvider.js`) — theoretically weaker than a CSPRNG but operationally unreachable within the rate-limited window; below the bar for a concrete exploit scenario.
- Technician-set spare-part pricing (`submitSpareParts`) — the reviewing subagent found this real (a technician can name their own price for a claimed part) but only at 7/10 confidence; per the skill's stated ≥8 threshold this was excluded rather than included on a hunch. Worth a human look later, not treated as confirmed here.

**Also fixed, found independently while auditing config for the security pass (not from the skill's diff-based findings — this predated the diff and was fixed first):** `config/env.js` previously fell back to the hardcoded dev JWT secrets (`'dev-only-access-secret'` / `'dev-only-refresh-secret'`) in *every* environment including production — since that default is public (committed to git), a production deployment that forgot to set `JWT_ACCESS_SECRET`/`JWT_REFRESH_SECRET` would silently boot with a forgeable secret, letting anyone mint a valid token for any role including `super_admin`. Now throws a fatal startup error in production (`NODE_ENV=production`) instead of falling back; development keeps the console-warn-and-fall-back behavior for local DX. Regression test: `tests/env.test.js` (4 cases via `spawnSync` on a real child process, controlling env vars precisely).

**Accepted, not fixed**: `npm audit` reports 3 moderate vulnerabilities, all inside `autocannon`'s (Phase 10 load-test tool) dev-only dependency chain — `npm audit --omit=dev` reports 0 vulnerabilities in production dependencies. Force-downgrading `autocannon` to clear the audit would risk breaking `scripts/loadtest.js` for a devDependency-only finding with no production exposure; documented here as a conscious accept, not an oversight.
- **`autocannon` added as a devDependency; `scripts/loadtest.js` + `npm run loadtest`** load-tests the hottest write path (`POST /bookings`, which itself exercises the real weighted assignment engine) against a real running server (not Jest/supertest's in-process simulation) — 10 connections × 10s. Results from a local run: **491.6 req/s average, p99 latency 32ms, 0 errors, 4916/4916 requests 2xx.** Not part of the CI/Jest gate (a manual diagnostic script, run against a disposable local DB, never Atlas/prod).
