# Phase 13 — real backend integration. Points the customer-facing app at a
# running backend/ instance. Copy to .env for local dev (already gitignored).
VITE_API_BASE_URL=http://localhost:4000/api/v1
